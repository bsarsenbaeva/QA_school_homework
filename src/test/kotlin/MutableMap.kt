import io.kotlintest.shouldBe
import io.kotlintest.shouldNotBe
import io.kotlintest.specs.DescribeSpec

class MutableMapSpec : DescribeSpec({
    describe("Home work 4") {
        context("binary sum") {

            val first_map= mapOf<String, String>("Australia" to "Canberra")
            val second_map= mapOf<String, String>("Italy" to "Rome")


        }
    }
})
//we have two binary numbers which consist of three element
//we need to find sum of this number. Example: 100+101=1001, 101+101=1010
fun capitals(map:MutableMap<String, String>){




}